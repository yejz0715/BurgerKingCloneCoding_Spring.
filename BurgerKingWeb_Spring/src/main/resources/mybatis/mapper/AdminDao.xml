<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ezen.burger.dao.IAdminDao">
	<select id="adminCheck" resultType="com.ezen.burger.dto.AdminVO">
		select * from admin where id=#{param1}
	</select>
	
	<select id="getAllCount" resultType="String">
		
	</select>
	
	<select id="listEvent" resultType="com.ezen.burger.dto.IAdminVO">
		 select * from ( 
		select * from(
			select rownum as rn, p.* from ( (select * from event where subject  like '%'||#{param2}||'%'   
			order by pseq desc) p )
		) where rn &gt;= #{param1.startNum} 
		) where rn &lt;= #{param1.endNum}  
	</select>
</mapper>